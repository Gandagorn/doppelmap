.superpowers/
node_modules/
dist/
__pycache__/
*.pyc
.pytest_cache/

# Raw per-image embedding caches (hundreds of MB, regenerable from the
# notebook's own cache, and past GitHub's 100MB per-file limit anyway) --
# only the aggregated data/prototypes.npz is meant to be committed.
data/emb_store.npz
data/emb_cache.npz

# Staging area for downloads pulled from the Commons collector run.
data/incoming/

# Per-person Commons face embeddings: ~170MB and growing, regenerated by
# the collector notebook. Only the built graph.json/photos.json are shipped.
data/faces/
data/faces_new/
data/incoming*/
data/*.zip
